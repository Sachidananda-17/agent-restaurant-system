# Script to get agent addresses
from alice import alice
from bob import bob

print("=== AGENT ADDRESSES ===")
print(f"Alice address: {alice.address}")
print(f"Bob address: {bob.address}")
print("========================")
